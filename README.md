# Интернет-магазин спортивного питания

**Автор:** Валиев И. Б.  
**Группа:** 036-22 SMMr

## Описание проекта

Современный интернет-магазин спортивного питания с полнофункциональной системой управления базой данных MySQL. Проект демонстрирует навыки работы с базами данных, создание качественного MVP и современный веб-дизайн 2025 года.

## Основные возможности

### Для покупателей:
- 🛍️ Просмотр каталога товаров спортивного питания
- 🔍 Поиск товаров по названию и описанию
- 📂 Фильтрация по категориям
- 🛒 Корзина покупок с управлением количеством
- 💰 Цены в узбекских сумах (UZS)
- 📱 Адаптивный дизайн для всех устройств
- ✨ Современный UI с нейроморфизмом и glassmorphism

### Для администраторов:
- ➕ Добавление новых товаров
- ✏️ Редактирование существующих товаров
- 🗑️ Удаление товаров
- 📊 Просмотр и управление заказами
- 🔄 Изменение статусов заказов
- 📈 Статистика продаж

## Технологии

### Backend:
- PHP 8.2+
- MySQL 8.0+
- PDO для работы с БД
- MVC архитектура

### Frontend:
- HTML5 с семантической разметкой
- CSS3 (Grid, Flexbox, Custom Properties)
- Vanilla JavaScript (ES6+)
- Современный дизайн 2025 года

### Безопасность:
- Подготовленные SQL-запросы (защита от SQL-инъекций)
- Валидация всех входных данных
- XSS защита
- Транзакции для целостности данных

## Установка

### Требования:
- PHP 8.2 или выше
- MySQL 8.0 или выше
- Apache/Nginx веб-сервер

### Шаги установки:

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd sports-nutrition-store
```

2. **Настройте базу данных:**
```bash
# Создайте базу данных MySQL
mysql -u root -p

# В MySQL консоли:
CREATE DATABASE sports_nutrition_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

3. **Установите схему базы данных:**
```bash
php database/install.php
```

Это автоматически создаст все необходимые таблицы и загрузит начальные данные.

4. **Настройте конфигурацию:**

Отредактируйте `src/config/database.php` если необходимо изменить параметры подключения к БД:
```php
private static $host = 'localhost';
private static $dbname = 'sports_nutrition_store';
private static $username = 'root';
private static $password = '';
```

5. **Настройте веб-сервер:**

Укажите `public/` как корневую директорию сайта.

Пример конфигурации Apache:
```apache
<VirtualHost *:80>
    DocumentRoot "/path/to/sports-nutrition-store/public"
    ServerName sports-nutrition.local
    
    <Directory "/path/to/sports-nutrition-store/public">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

## Быстрый запуск

### Способ 1: Через Node.js (рекомендуется)
```bash
# Запуск на порту 8080 (по умолчанию)
node server.js

# Запуск на другом порту
PORT=9000 node server.js
```

### Способ 2: Через batch файл (Windows)
```cmd
# Запуск на порту 8080
start.bat

# Запуск на порту 9000
start.bat 9000
```

### Способ 3: Через PowerShell (Windows)
```powershell
# Запуск на порту 8080
.\Start-SportShop.ps1

# Запуск на порту 9000 с автоматическим открытием браузера
.\Start-SportShop.ps1 -Port 9000 -OpenBrowser
```

После запуска откройте браузер и перейдите по адресу: **http://localhost:8080**

## Структура проекта

```
sports-nutrition-store/
├── public/                 # Публичная директория
│   ├── index.php          # Главный файл приложения
│   ├── views/             # HTML шаблоны
│   ├── assets/            # Статические файлы
│   │   ├── css/          # Стили
│   │   ├── js/           # JavaScript
│   │   └── images/       # Изображения
│   └── admin/            # Административная панель
├── src/                   # Исходный код
│   ├── config/           # Конфигурация
│   ├── models/           # Модели данных
│   ├── controllers/      # Контроллеры
│   ├── repositories/     # Репозитории для работы с БД
│   └── services/         # Сервисы бизнес-логики
├── database/             # База данных
│   ├── schema.sql       # Схема БД
│   ├── seeds.sql        # Начальные данные
│   └── install.php      # Скрипт установки
├── tests/               # Тесты
│   └── property/        # Property-based тесты
└── logs/                # Логи приложения
```

## Использование

### Для покупателей:

1. Откройте главную страницу сайта
2. Просмотрите каталог товаров
3. Используйте поиск или фильтры по категориям
4. Добавьте товары в корзину
5. Оформите заказ, указав контактные данные

### Для администраторов:

1. Перейдите на `/admin/login`
2. Войдите с учетными данными:
   - Логин: `admin`
   - Пароль: `admin123`
3. Управляйте товарами и заказами через панель администратора

## API Endpoints

### Товары:
- `GET /api/products` - Получить список товаров
- `GET /api/products?category=1` - Фильтр по категории
- `GET /api/products?search=protein` - Поиск товаров
- `POST /api/products` - Создать товар (админ)

### Корзина:
- `GET /api/cart` - Получить корзину
- `POST /api/cart/add` - Добавить товар
- `POST /api/cart/update` - Обновить количество
- `POST /api/cart/remove` - Удалить товар

### Заказы:
- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Получить заказы (админ)
- `POST /api/orders/status` - Обновить статус (админ)

## Тестирование

Проект включает comprehensive property-based тесты:

```bash
# Запуск всех тестов
php tests/property/DatabaseInitializationTest.php
php tests/property/PreparedStatementsTest.php
php tests/property/LoggingTest.php
php tests/property/ProductRepositoryTest.php
php tests/property/CartServiceTest.php
php tests/property/OrderServiceTest.php
php tests/property/AdminOrderDisplayTest.php
php tests/property/ValidationSerializationTest.php
```

## Особенности реализации

### Работа с базой данных:
- Все запросы используют подготовленные выражения (PDO)
- Транзакции для критических операций
- Автоматическое логирование всех операций
- Оптимизированные индексы для быстрого поиска

### Валюта:
- Все цены в узбекских сумах (UZS)
- Правильное форматирование с разделителями тысяч
- Валидация ввода цен

### Дизайн:
- Нейроморфизм для карточек товаров
- Glassmorphism для модальных окон
- Плавные анимации и переходы
- Адаптивная верстка (mobile-first)

## Автор

**Валиев И. Б.**  
Группа: 036-22 SMMr

Проект выполнен в рамках учебного задания по созданию веб-приложения с подключенной базой данных.

## Лицензия

Этот проект создан в образовательных целях."# valiev" 
